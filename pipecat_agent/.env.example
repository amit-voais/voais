# Point these at the GPU box. With the IAP tunnel open:
#   gcloud compute ssh voais-a1 --zone asia-south1-a --tunnel-through-iap -- \
#     -L 8000:localhost:8000 -L 8002:localhost:8002
VLLM_URL=http://localhost:8000
TTS_URL=http://localhost:8002
F5_NFE_STEP=8
